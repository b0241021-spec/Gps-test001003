name: Android AI Auto-Healing Builder

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  auto_healing:
    runs-on: ubuntu-latest  # <-- 請將原先的 ubuntu-21.04 改成這一行！
    steps:
      - name: 1. 檢查並自我修復被快取污染的腳本
        run: |
